# Core
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
gunicorn==21.2.0
python-dotenv==1.0.0

# Database driver (PostgreSQL)
psycopg2-binary==2.9.9

# Migrations (optional, useful if you want schema updates)
Flask-Migrate==4.0.4

# CORS (needed if your frontend is separate)
Flask-Cors==3.0.10

# Optional (for internal utilities)
Werkzeug==2.3.7
